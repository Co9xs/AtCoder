## 累積和の利用
ある配列における区間(l, r)の連続部分は、区間(1, r) - 区間(1, l)で求められることを利用する
下記の2手順を分けて考える
- 累積和の配列の作成<br>
  長さNの配列に対して、区間(0,0)から区間(0,N)までの長さ(N+1)の累積和の配列を生成
  
- 与えられた範囲のクエリの実行<br>
  求めたい範囲(l, r)が各クエリとして与えられるので、区間(1, r) - 区間(1, l)をそれぞれ求めていく<br>
  ※配列の添字とのズレに注意して実装する
